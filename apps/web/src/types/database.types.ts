// Shim: the generated Supabase types moved to @jambahr/supabase (PRD-01 Phase B).
// Kept so existing `@/types/database.types` imports keep working unchanged.
export type { Json, Database } from "@jambahr/supabase";
