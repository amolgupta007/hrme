// @jambahr/supabase — generated Database types + client-injected query helpers.
// Regenerate database.types.ts via `npm run db:generate` in apps/web.
export type { Json, Database } from "./database.types";
